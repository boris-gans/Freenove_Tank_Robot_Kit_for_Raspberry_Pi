# ============================================================
# Freenove Tank Robot — Python dependencies
#
# IMPORTANT: your venv MUST be created with --system-site-packages
# so that apt-installed packages (opencv, picamera2, lgpio, etc.)
# are visible inside it. If you get ModuleNotFoundError for cv2,
# lgpio, or picamera2, recreate the venv:
#
#   rm -rf .venv
#   python3 -m venv --system-site-packages .venv
#   source .venv/bin/activate
#
# STEP 1 — install system packages (do this before pip):
#
#   sudo apt update
#   sudo apt install -y \
#       python3-opencv \
#       python3-picamera2 \
#       python3-gpiozero \
#       python3-lgpio \
#       python3-pyqt5
#
# STEP 2 — install the remaining pure-Python packages:
#
#   pip install -r Code/Client/requirements.txt
#
# ============================================================

# ── Pure-Python / reliably pip-installable ───────────────────
numpy>=1.21.0
Pillow>=9.0.0
rpi-hardware-pwm>=0.1.4
pigpio>=1.78
setuptools>=60.0.0

# ── DO NOT add these here — they must come from apt (STEP 1) ─
# Adding them causes pip to try building C extensions from source
# which fails without swig/liblgpio-dev/cmake etc.
#
#   opencv-python  ->  sudo apt install python3-opencv
#   picamera2      ->  sudo apt install python3-picamera2
#   gpiozero       ->  sudo apt install python3-gpiozero
#   lgpio          ->  sudo apt install python3-lgpio
#   PyQt5          ->  sudo apt install python3-pyqt5
